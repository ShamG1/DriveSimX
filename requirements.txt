# Python 3.8 stable constraints (avoid future major jumps)
numpy>=1.23,<2.0
pybind11>=2.11,<3.1
pillow>=9.2,<11.0
torch>=2.3,<2.5

# Packaging/build helpers
setuptools>=65,<76
wheel>=0.38,<0.46

# OpenGL/GLFW are primarily linked from system/C++ side.
# On Linux, install system glfw/OpenGL dev packages.
# On Windows, provide GLFW binaries as described in README.
